---
apiVersion: v1
kind: Service
metadata:
  labels:
    molior.service: web
  name: web
spec:
  type: NodePort
  ports:
    - name: "web-ui"
      port: 8000
      targetPort: 80
      nodePort: 30080
  selector:
    molior.service: web
