apiVersion: v1
kind: Service
metadata:
  labels:
    molior.service: registry
  name: registry
spec:
  ports:
    - name: "5000"
      port: 5000
      targetPort: 5000
  selector:
    molior.service: registry
