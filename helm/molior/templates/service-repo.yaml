apiVersion: v1
kind: Service
metadata:
  labels:
    molior.service: repo
  name: repo
spec:
  type: NodePort
  ports:
    - name: "8000"
      port: 8000
      targetPort: 8000
      nodePort: 30080
  selector:
    molior.service: aptly
